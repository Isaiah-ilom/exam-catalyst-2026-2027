{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install --production=false && cd backend && npm install --production=false && cd ../frontend && npm install --production=false && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}